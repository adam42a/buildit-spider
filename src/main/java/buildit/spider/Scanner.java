package buildit.spider;

/**
 * This will scan the HTML of each page on the website, converting it into
 * a {@link SiteNode} populated with child pages, static content and external
 * URLs.
 * 
 * 
 *
 */
public class Scanner {

    public SiteNode scan(SiteNode node, String htmlSource) {
        return node;
        
    }

}
